# Get a person by its ID
query get_person($id: ID!) {
    person(personID: $id) {
        name
        birthYear
        eyeColor
        hairColor
        height
        mass
        skinColor
        homeworld {
            name
        }
    }
}
